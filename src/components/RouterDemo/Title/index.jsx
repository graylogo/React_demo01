import React, { Component } from 'react'

export default class Title extends Component {
    render() {
        return (
            <div>
                这是个一般组件，直接使用。<br/>
                下面是路由组件，需要在注册路由时候使用
            </div>
        )
    }
}
